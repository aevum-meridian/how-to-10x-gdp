# Larastan Level 10 — no baseline, no suppression (DEV-0 quality gates).
includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 10
    paths:
        - app
    tmpDir: storage/framework/phpstan
